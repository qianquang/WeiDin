# 微钉即时通讯系统 — 项目架构说明文档

> **最后更新**：2026 年 6 月

---

## 目录

- [一、项目概述](#一项目概述)
- [二、技术架构](#二技术架构)
  - [整体架构图](#整体架构图)
  - [模块依赖关系](#模块依赖关系)
- [三、模块详细说明](#三模块详细说明)
  - [WeiDin.Core — 核心层/领域层](#1-weidincore核心层领域层)
  - [WeiDin.Application — 应用层](#2-weidinapplication应用层)
  - [WeiDin.Infrastructure — 基础设施层](#3-weidininfrastructure基础设施层)
  - [WeiDin.API — API 层](#4-weidinapi-api-层)
  - [WeiDin.Frontend — 前端层](#5-weidinfrontend前端层)
- [四、数据流向](#四数据流向)
- [五、前后端通信规范](#五前后端通信规范)
  - [RESTful API 通信](#51-restful-api-通信)
  - [API 端点清单](#52-api-端点清单)
  - [HTTP 错误码规范](#53-http-错误码规范)
- [六、SignalR 实时通信规范](#六signalr-实时通信规范)
  - [连接配置](#61-连接配置)
  - [事件转发架构](#62-事件转发架构)
  - [服务端推送事件](#63-服务端推送事件server--client)
  - [客户端调用方法](#64-客户端调用方法client--server)
  - [SignalR 分组策略](#65-signalr-分组策略)
  - [在线状态管理机制](#66-在线状态管理机制)
- [七、RAG 知识库系统](#七rag-知识库系统)
  - [系统架构](#71-系统架构)
  - [RAG 流水线](#72-rag-流水线)
  - [文档摄取管道](#73-文档摄取管道)
  - [向量存储与检索](#74-向量存储与检索)
  - [LLM 集成](#75-llm-集成)
- [八、核心数据流时序](#八核心数据流时序)
- [九、数据类型定义（前后端对照）](#九数据类型定义前后端对照)
- [十、前端状态管理与通信职责](#十前端状态管理与通信职责)
- [十一、开发规范](#十一开发规范)

---

## 一、项目概述

微钉即时通讯系统是一个基于 **ASP.NET Core 8.0** 和 **Vue.js 3** 开发的现代化即时通讯平台。项目采用领域驱动设计（DDD）和分层架构模式，使用 ABP Framework 作为基础框架，实现了用户管理、消息发送、群组管理、好友管理等完整功能。此外，系统内置了完整的 **RAG（Retrieval-Augmented Generation）知识库引擎**，支持文档上传、向量化检索、交叉编码重排和 LLM 智能问答。

**核心特性**：

| 特性 | 说明 |
|------|------|
| 清晰的分层架构 | DDD 分层，各模块职责明确，高内聚低耦合 |
| 实时通信 | SignalR WebSocket 推送，毫秒级消息送达 |
| 安全性 | JWT 认证、密码加密、权限控制 |
| 高性能 | Redis 缓存、动态消息分表 |
| RAG 知识库 | 6 阶段流水线：查询理解 → 向量检索 → 重排 → 上下文组装 → LLM 生成 |
| 本地模型推理 | ONNX Runtime 运行嵌入模型和重排模型，无外部 API 依赖 |
| 可扩展 | 模块化设计，接口解耦，易于扩展新功能 |

---

## 二、技术架构

### 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                       WeiDin.Frontend                             │
│   Vue.js 3 + Element Plus + Pinia + SignalR Client               │
│   Three-layer realtime: Adapter → EventBus → Handlers            │
└──────────────────────────┬───────────────────────────────────────┘
                           │ HTTP / WebSocket
┌──────────────────────────▼───────────────────────────────────────┐
│                         WeiDin.API                                │
│   ASP.NET Core 8.0 + SignalR + JWT + Swagger + Serilog           │
├──────────┬────────────────┬───────────────────┬──────────────────┤
│          │                │                   │                  │
│ ┌────────▼────────┐ ┌─────▼──────────┐ ┌─────▼──────────────┐  │
│ │  Application    │ │ Infrastructure │ │      Core           │  │
│ │     Layer       │ │    Layer       │ │     Layer           │  │
│ │                 │ │                │ │                     │  │
│ │ • IM Services   │ │ • EF Core      │ │ • IM Entities       │  │
│ │ • RAG Engine    │ │ • ONNX Runtime │ │ • Knowledge Entities│  │
│ │ • LLM Service   │ │ • Vector Store │ │ • Enums             │  │
│ │ • Retrieval     │ │ • Doc Parser   │ │ • Interfaces        │  │
│ │ • Rerank        │ │ • Dynamic Table│ │                     │  │
│ └─────────────────┘ └────────────────┘ └─────────────────────┘  │
│                                                                  │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │                   External Services                           │ │
│ │   SQL Server · Redis · Qdrant · DeepSeek API · ONNX Models    │ │
│ └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 模块依赖关系

```
WeiDin.Frontend
    │
    │  (HTTP / WebSocket)
    │
WeiDin.API
    │
    ├───────────────┐
    │               │
WeiDin.Application  WeiDin.Infrastructure
    │               │
    └───────┬───────┘
            │
      WeiDin.Core
```

| 模块 | 依赖 | 说明 |
|------|------|------|
| **WeiDin.Core** | 无 | 最底层，被所有后端模块依赖 |
| **WeiDin.Application** | Core | 通过接口解耦，不直接依赖 Infrastructure |
| **WeiDin.Infrastructure** | Core | 提供 Core 接口的具体实现（数据访问、AI 推理） |
| **WeiDin.API** | Application + Infrastructure | 系统入口，组装所有模块 |
| **WeiDin.Frontend** | — | 通过 HTTP/WebSocket 与 API 通信 |

---

## 三、模块详细说明

### 1. WeiDin.Core（核心层/领域层）

**职责**：定义领域实体、值对象、枚举、输入模型和核心接口，是系统的核心业务模型，不依赖任何其他模块。

**技术栈**：ABP Framework DDD Domain 模块、Entity Framework Core（仅用于实体定义）

**目录结构与关键文件**：

```
WeiDin.Core/
├── WeiDinCoreModule.cs          # 核心模块配置
├── Entities/                    # 领域实体
│   ├── User.cs                  # 用户实体
│   ├── Message.cs               # 消息实体
│   ├── MessageStatus.cs         # 消息状态实体
│   ├── MessageAttachment.cs     # 消息附件实体
│   ├── Conversation.cs          # 消息关系实体（关联 RelationId 与动态分表）
│   ├── Group.cs                 # 群组实体
│   ├── GroupMember.cs           # 群组成员实体
│   ├── Friendship.cs            # 好友关系实体
│   ├── Blacklist.cs             # 黑名单实体
│   ├── KnowledgeBase.cs         # 知识库实体
│   ├── Knowledge.cs             # 知识文档实体
│   └── KnowledgeChunk.cs        # 知识分块实体
├── Enums/
│   ├── RelationType.cs          # 关系类型枚举（Friendship / Group）
│   ├── KnowledgeChunkType.cs    # 知识分块类型（ChatMessage / UserProfile / GroupInfo / FileContent / Custom）
│   ├── ParseStatus.cs           # 文档解析状态（Pending / Processing / Completed / Failed）
│   ├── IndexingStrategy.cs      # 索引策略（Vector / Keyword / Wiki / Graph）
│   └── SummaryStatus.cs         # 摘要状态（None / Pending / Completed）
├── Inputs/                      # 领域层输入模型
│   ├── CreateMessageInput.cs    # 创建消息输入
│   └── CreateMessageAttachmentInput.cs  # 创建消息附件输入
└── Interfaces/                  # 核心接口定义
    ├── IRepository.cs           # 通用泛型仓储接口
    ├── IDynamicMessageRepository.cs  # 动态消息分表仓储接口
    └── IDynamicTableService.cs  # 动态分表管理接口
```

**关键设计说明**：

- `Conversation` 实体是消息分表的核心，每段好友/群聊关系对应一条记录，通过 `RelationId` 关联动态分表 `Message_{RelationId}`、`MessageAttachment_{RelationId}`、`MessageStatus_{RelationId}`
- `RelationType` 枚举区分 `Friendship`（私聊）和 `Group`（群聊）两种关系类型
- `IDynamicMessageRepository` 提供基于分表的消息 CRUD 操作
- `IDynamicTableService` 负责动态分表的创建、同步和删除
- `IRepository<T>` 提供通用的泛型仓储接口（GetById、Find、Add、Update、Delete 等）

**知识库实体关系**：

```
KnowledgeBase (知识库)
  │
  ├── 1:N ── Knowledge (知识文档)
  │              │
  │              └── 1:N ── KnowledgeChunk (知识分块)
  │
  └── 1:N ── KnowledgeChunk (直接关联的知识分块)
```

- `KnowledgeBase`：知识库容器，包含名称、描述、索引策略、分块数量、启用状态
- `Knowledge`：代表一个上传的文档文件，包含解析状态（ParseStatus）和摘要状态（SummaryStatus）
- `KnowledgeChunk`：向量检索的基本单元，存储文本内容、元数据、分块索引、来源信息。支持双向链表结构（PreChunkId / NextChunkId）用于保持上下文顺序。向量存储在独立的向量数据库中，不在 SQL Server 中

---

### 2. WeiDin.Application（应用层）

**职责**：实现业务逻辑，协调领域对象完成业务用例。依赖 Core 层，通过接口解耦不直接依赖 Infrastructure。

**技术栈**：ABP Framework DDD Application 模块、AutoMapper 12.0.1、FluentValidation 11.8.1、Microsoft Semantic Kernel 1.77.0

**目录结构与关键文件**：

```
WeiDin.Application/
├── WeiDinApplicationModule.cs   # 应用模块配置，注册 AutoMapper
├── Services/                    # 应用服务实现
│   ├── UserService.cs           # 用户业务逻辑（注册、登录、JWT 生成）
│   ├── MessageService.cs        # 消息业务逻辑（发送、查询、状态管理）
│   ├── GroupService.cs          # 群组业务逻辑（创建、加入、成员管理）
│   ├── FriendshipService.cs     # 好友关系业务逻辑（申请、接受、黑名单）
│   ├── AgentService.cs          # RAG 问答入口服务
│   ├── KnowledgeService.cs      # 知识库管理服务（CRUD、文档上传、分块管理）
│   ├── RagAgentEngine.cs        # RAG 流水线引擎（6 阶段编排）
│   ├── QueryUnderstandingService.cs  # 查询理解（LLM 意图分类 + 查询改写）
│   ├── RetrievalService.cs      # 向量检索服务（嵌入 + 搜索）
│   ├── OnnxRerankService.cs     # 交叉编码重排服务（bge-reranker-base）
│   ├── TextChunkingService.cs   # 文本分块服务（段落分割、合并、重叠窗口）
│   ├── DocumentParser.cs        # 文档解析器（txt/md/pdf/docx/代码文件）
│   ├── DeepSeekLlmService.cs    # DeepSeek LLM 服务（Semantic Kernel OpenAI 兼容）
│   └── SummaryService.cs        # 文档摘要服务
├── DTOs/                        # 数据传输对象
│   ├── UserDto.cs               # 用户相关 DTO
│   ├── MessageDto.cs            # 消息相关 DTO
│   ├── GroupDto.cs              # 群组相关 DTO
│   ├── FriendshipDto.cs         # 好友关系相关 DTO
│   ├── KnowledgeBaseDto.cs      # 知识库相关 DTO
│   └── KnowledgeChunkDto.cs     # 知识分块相关 DTO
├── Interfaces/                  # 服务接口定义
│   ├── IUserService.cs
│   ├── IMessageService.cs
│   ├── IGroupService.cs
│   ├── IFriendshipService.cs
│   ├── IAgentService.cs
│   ├── IKnowledgeService.cs
│   ├── IRagAgentEngine.cs
│   ├── IQueryUnderstandingService.cs
│   ├── IRetrievalService.cs
│   ├── IRerankService.cs
│   ├── ILlmService.cs
│   ├── IEmbeddingService.cs
│   ├── ITextChunkingService.cs
│   └── IDocumentParser.cs
└── Mappings/
    └── MappingProfile.cs        # AutoMapper 映射配置
```

**RAG 相关服务说明**：

| 服务 | 职责 |
|------|------|
| `RagAgentEngine` | RAG 流水线总编排，串联查询理解 → 意图路由 → 检索 → 重排 → 上下文组装 → LLM 生成 |
| `QueryUnderstandingService` | 使用 LLM 改写模糊查询、分类意图（KnowledgeSearch / DirectChat） |
| `RetrievalService` | 调用嵌入模型将查询向量化，在向量存储中搜索 3x TopK 候选 |
| `OnnxRerankService` | 使用 bge-reranker-base 交叉编码器对查询-文档对重新打分 |
| `TextChunkingService` | 段落分割 → 合并至目标大小 → 超长句切分 → 重叠窗口 |
| `DocumentParser` | 支持 .txt、.md、.pdf（PdfPig）、.docx（OpenXml）及 20+ 种代码/配置格式 |
| `DeepSeekLlmService` | 通过 Semantic Kernel 的 OpenAI 兼容接口调用 DeepSeek API，支持思维/推理模式 |
| `KnowledgeService` | 知识库 CRUD、文档上传与异步摄取、分块管理 |
| `SummaryService` | 文档摘要生成 |

---

### 3. WeiDin.Infrastructure（基础设施层）

**职责**：提供技术实现，包括数据访问、动态分表管理、ONNX 模型推理、内存向量存储、文档解析等。依赖 Core 层。

**技术栈**：Entity Framework Core 8.0.8、SQL Server、ABP Framework EF Core 模块、StackExchange.Redis、Microsoft.ML.OnnxRuntime 1.18.1

**目录结构与关键文件**：

```
WeiDin.Infrastructure/
├── WeiDinInfrastructureModule.cs    # 基础设施模块配置（EF Core、数据库连接）
├── Data/
│   └── WeiDinDbContext.cs           # EF Core 数据上下文
├── Repositories/                    # 仓储实现
│   ├── Repository.cs                # 通用泛型仓储实现（IRepository<T>）
│   ├── DynamicMessageRepository.cs  # 动态消息分表仓储实现
│   └── DynamicTableService.cs       # 动态分表管理实现（建表、同步结构、删表）
├── AI/                              # AI/ML 基础设施
│   ├── OnnxEmbeddingService.cs      # ONNX 嵌入服务（BGE-small-zh-v1.5）
│   ├── OnnxRerankService.cs         # ONNX 重排服务（bge-reranker-base）
│   ├── BertTokenizer.cs             # BERT WordPiece 分词器（支持中文）
│   ├── InMemoryVectorStore.cs       # 内存向量存储（开发用，Semantic Kernel VectorStore 实现）
│   └── QdrantVectorStore.cs         # Qdrant 向量存储（生产用，基于 Qdrant.Client gRPC）
└── Migrations/                      # EF Core 数据库迁移文件
```

**关键设计说明**：

- `Repository<T>` 是通用泛型仓储，基于 EF Core `DbSet<T>` 实现标准 CRUD
- `DynamicTableService` 负责按 `ConversationId` 动态创建/删除 `Message_xxx`、`MessageAttachment_xxx`、`MessageStatus_xxx` 三张分表，并支持主表结构同步到分表
- `DynamicMessageRepository` 基于分表进行消息的增删改查
- `OnnxEmbeddingService`：完整的 BERT WordPiece 分词器实现，支持中文字符级处理，均值池化 + L2 归一化，输出 512 维向量
- `OnnxRerankService`：使用 bge-reranker-base 交叉编码模型对查询-文档对重新评分，精度高于双编码器
- `InMemoryVectorStore`：自定义实现 Semantic Kernel 的 `VectorStore` 抽象类，使用余弦相似度暴力搜索，适合开发环境
- `QdrantVectorStore`：基于 `Qdrant.Client` gRPC 实现的 `VectorStore` 抽象类，连接 Qdrant 向量数据库，支持持久化存储、HNSW 索引和高效 ANN 搜索。通过 `appsettings.json` 中 `VectorStore:Provider` 配置项切换（`InMemory` / `Qdrant`）
- `BertTokenizer`：手写的 BERT WordPiece 分词器，支持中文字符级 tokenization，无外部依赖

---

### 4. WeiDin.API（API 层）

**职责**：作为系统入口层，负责处理 HTTP 请求、WebSocket 连接和系统配置。

**技术栈**：ASP.NET Core 8.0、ABP Framework 8.1.5、SignalR 1.1.0、JWT Bearer Authentication、Swagger/OpenAPI、Serilog、Redis 缓存

**目录结构与关键文件**：

```
WeiDin.API/
├── Program.cs                   # 应用程序入口（配置 Serilog 和 ABP 框架）
├── WeiDinApiModule.cs           # API 模块配置（JWT、CORS、Redis、SignalR）
├── Controllers/                 # RESTful API 控制器
│   ├── AuthController.cs        # 用户认证（注册、登录、登出）
│   ├── UsersController.cs       # 用户管理
│   ├── MessagesController.cs    # 消息管理
│   ├── GroupsController.cs      # 群组管理
│   ├── FriendshipsController.cs # 好友管理
│   ├── FilesController.cs       # 文件上传下载
│   ├── AgentController.cs       # RAG 问答接口
│   └── KnowledgeController.cs   # 知识库管理接口
├── Hubs/
│   └── ChatHub.cs               # SignalR Hub（实时消息推送、在线状态管理）
├── Scripts/                     # 数据库初始化脚本
│   ├── InitDatabase.sql
│   ├── SetupDatabase.bat
│   └── SetupDatabase.sh
└── Properties/
    └── launchSettings.json      # 启动配置
```

**依赖关系**：依赖 `WeiDin.Application`（应用服务层）和 `WeiDin.Infrastructure`（基础设施层）。

---

### 5. WeiDin.Frontend（前端层）

**职责**：提供用户界面，与后端 API 和 SignalR Hub 进行交互。

**技术栈**：

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue.js 3 | ^3.4.0 |
| 语言 | TypeScript | ~5.3.0 |
| UI 库 | Element Plus | ^2.4.4 |
| 图标 | @element-plus/icons-vue | ^2.3.1 |
| 状态管理 | Pinia | ^2.1.7 |
| 路由 | Vue Router | ^4.2.5 |
| HTTP 客户端 | Axios | ^1.6.2 |
| 实时通信 | @microsoft/signalr | ^8.0.0 |
| 日期处理 | Day.js | ^1.11.10 |
| 构建工具 | Vite | ^5.0.10 |

**目录结构与关键文件**：

```
WeiDin.Frontend/src/
├── main.ts                      # 应用入口
├── App.vue                      # 根组件（初始化认证和 SignalR 连接）
├── router/
│   └── index.ts                 # 路由配置 + 路由守卫
├── stores/                      # Pinia 状态管理
│   ├── auth.ts                  # 认证状态（登录、注册、Token 管理）
│   ├── chat.ts                  # 聊天状态（会话、消息、在线状态）
│   ├── friendship.ts            # 好友状态（好友列表、申请、黑名单）
│   ├── group.ts                 # 群组状态
│   ├── signalr.ts               # SignalR 连接生命周期管理
│   └── invalidation.ts          # 数据失效标记（控制页面刷新策略）
├── api/                         # API 客户端
│   ├── index.ts                 # 统一导出所有 API 模块
│   ├── request.ts               # Axios 请求封装（拦截器、Token 注入）
│   ├── auth.ts                  # 认证 API
│   ├── message.ts               # 消息 API
│   ├── group.ts                 # 群组 API
│   ├── friendship.ts            # 好友 API
│   ├── file.ts                  # 文件 API
│   └── knowledge.ts             # 知识库 API
├── realtime/                    # 实时通信层（三层解耦架构）
│   ├── adapter.ts               # SignalR 适配器（Hub 事件 → 事件总线）
│   ├── bus.ts                   # 发布/订阅事件总线（与 SignalR 解耦）
│   └── handlers.ts              # 事件处理器（事件总线 → Pinia Store）
├── types/
│   └── index.ts                 # TypeScript 类型定义（所有接口/类型）
├── utils/
│   ├── signalr.ts               # SignalR 客户端封装（useSignalR composable）
│   └── index.ts                 # 工具函数（时间格式化、防抖、节流、文件处理等）
└── views/                       # 页面组件
    ├── Login.vue                # 登录页
    ├── Register.vue             # 注册页
    ├── Chat.vue                 # 聊天页（主功能页）
    ├── Friends.vue              # 好友页
    ├── Groups.vue               # 群组页
    ├── Profile.vue              # 个人资料页
    ├── Settings.vue             # 设置页
    └── NotFound.vue             # 404 页面
```

**路由配置**：

| 路径 | 组件 | 需要认证 | 说明 |
|------|------|---------|------|
| `/` | — | — | 重定向到 `/login` |
| `/login` | Login.vue | 否 | 已登录自动跳转 `/chat` |
| `/register` | Register.vue | 否 | 已登录自动跳转 `/chat` |
| `/chat` | Chat.vue | 是 | 聊天主页 |
| `/friends` | Friends.vue | 是 | 好友管理 |
| `/groups` | Groups.vue | 是 | 群组管理 |
| `/profile` | Profile.vue | 是 | 个人资料 |
| `/settings` | Settings.vue | 是 | 设置页 |
| `/:pathMatch(.*)*` | NotFound.vue | — | 404 页面 |

---

## 四、数据流向

### 请求流程（用户主动操作）

```
用户操作 (Frontend)
    ↓
Vue 组件 → Pinia Store → API 模块 (Axios)
    ↓
HTTP 请求
    ↓
WeiDin.API (Controllers)
    ↓
WeiDin.Application (Services)
    ↓
WeiDin.Infrastructure (Repositories)
    ↓
数据库 (SQL Server)
```

### 响应流程

```
数据库 (SQL Server)
    ↓
WeiDin.Infrastructure (Repositories)
    ↓
WeiDin.Application (Services → DTO 转换)
    ↓
WeiDin.API (Controllers)
    ↓
HTTP 响应 (JSON)
    ↓
WeiDin.Frontend (Pinia Store → Vue 组件响应式更新)
```

### 实时推送流程

```
ChatHub (后端触发)
    ↓
SignalR WebSocket
    ↓
signalRAdapter (src/realtime/adapter.ts)
    ↓
realtimeBus (src/realtime/bus.ts)
    ↓
handlers.ts (src/realtime/handlers.ts)
    ↓
Pinia Store (chat / friendship / ...)
    ↓
Vue 组件自动响应式更新 UI
```

### RAG 问答流程

```
用户提问 (Frontend)
    ↓
POST /api/v1/agent/ask
    ↓
AgentController → AgentService → RagAgentEngine
    ↓
① QueryUnderstandingService (LLM 意图分类 + 查询改写)
    ↓
② RetrievalService (ONNX 嵌入 → 向量搜索)
    ↓
③ OnnxRerankService (交叉编码重排)
    ↓
④ 上下文组装
    ↓
⑤ DeepSeekLlmService (LLM 生成回答)
    ↓
返回答案 + 引用来源
```

---

## 五、前后端通信规范

### 5.1 RESTful API 通信

#### 基础配置

| 配置项 | 值 | 说明 |
|-------|-----|------|
| Base URL | `/api/v1` | 所有 API 的统一前缀 |
| Content-Type | `application/json` | 默认请求格式（文件上传除外） |
| 认证方式 | `Bearer Token` (JWT) | 通过 `Authorization` 请求头传递 |
| 超时时间 | 10000ms | Axios 默认超时 |
| 开发代理 | `https://localhost:7000` | Vite 代理转发到后端 |

#### 请求/响应拦截器（`src/api/request.ts`）

- **请求拦截**：自动从 `authStore.token` 读取 JWT Token 并注入 `Authorization: Bearer {token}` 请求头
- **响应拦截**：统一处理错误码（400/401/403/404/500），通过 `ElMessage` 展示错误提示
- **401 处理**：自动触发 `authStore.logout()` 清除登录状态

#### 序列化规范

| 层 | 命名风格 | 示例 |
|----|---------|------|
| C# 属性 | PascalCase | `SenderName` |
| JSON 响应 | camelCase（ASP.NET Core 自动转换） | `senderName` |
| TypeScript 接口 | camelCase | `senderName` |

---

### 5.2 API 端点清单

#### 认证模块（`AuthController`）— 无需认证

| 方法 | 路径 | 说明 | 请求体 | 响应体 |
|------|------|------|--------|--------|
| POST | `/api/v1/auth/register` | 用户注册 | `CreateUserDto` | `AuthResponse { user, token }` |
| POST | `/api/v1/auth/login` | 用户登录 | `LoginDto { username, password }` | `AuthResponse { user, token }` |
| POST | `/api/v1/auth/logout` | 用户登出 | `{ userId: string }` | `"退出成功"` |

#### 用户模块（`UsersController`）

| 方法 | 路径 | 认证 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------|------------|--------|
| GET | `/api/v1/users` | 否 | 获取所有用户 | — | `User[]` |
| GET | `/api/v1/users/{id}` | 否 | 获取用户详情 | Path: `id` | `User` |
| POST | `/api/v1/users` | 否 | 创建用户 | `CreateUserDto` | `User` |
| PUT | `/api/v1/users/{id}` | 否 | 更新用户信息 | `UpdateUserDto` | `User` |
| DELETE | `/api/v1/users/{id}` | 否 | 删除用户 | Path: `id` | 204 |
| POST | `/api/v1/users/{id}/change-password` | 否 | 修改密码 | `ChangePasswordDto` | `"密码修改成功"` |
| POST | `/api/v1/users/{id}/online-status` | 否 | 设置在线状态 | `boolean` | `"状态更新成功"` |

#### 消息模块（`MessagesController`）— 需要认证

| 方法 | 路径 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------------|--------|
| GET | `/api/v1/messages/relation/{relationId}` | 分页查询消息 | Query: `page`, `pageSize` | `Message[]` |
| GET | `/api/v1/messages/relation/{relationId}/{id}` | 查询单条消息 | Path 参数 | `Message` |
| POST | `/api/v1/messages` | 发送消息 | `CreateMessageDto` | `Message` (201) |
| DELETE | `/api/v1/messages/relation/{relationId}/{id}` | 删除消息 | Path 参数 | 204 |
| PUT | `/api/v1/messages/relation/{relationId}/{id}/status` | 更新消息状态 | `UpdateMessageStatusDto` | `"状态更新成功"` |
| POST | `/api/v1/messages/relation/{relationId}/{id}/read` | 标记已读 | Path 参数 | `"已标记为已读"` |
| POST | `/api/v1/messages/relation/{relationId}/{id}/delivered` | 标记已送达 | Path 参数 | `"已标记为已送达"` |
| GET | `/api/v1/messages/relation/{relationId}/search` | 搜索消息 | Query: `keyword`, `page`, `pageSize` | `Message[]` |

> **关键概念**：`relationId` 是会话标识（私聊为 `Friendship.Id`，群聊为 `Group.Id`），消息按 `relationId` 存储在动态分表 `Message_{relationId}`、`MessageAttachment_{relationId}`、`MessageStatus_{relationId}` 中。

#### 好友模块（`FriendshipsController`）— 需要认证

| 方法 | 路径 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------------|--------|
| GET | `/api/v1/friendships` | 获取好友列表 | — | `Friendship[]` |
| GET | `/api/v1/friendships/{id}` | 获取好友详情 | Path: `id` | `Friendship` |
| POST | `/api/v1/friendships` | 发送好友申请 | `CreateFriendshipDto` | `Friendship` (201) |
| DELETE | `/api/v1/friendships/{id}` | 删除好友 | Path: `id` | 204 |
| PUT | `/api/v1/friendships/{id}` | 更新好友信息 | `UpdateFriendshipDto` | `Friendship` |
| POST | `/api/v1/friendships/{id}/accept` | 接受好友申请 | Path: `id` | `Friendship` |
| POST | `/api/v1/friendships/{id}/reject` | 拒绝好友申请 | Path: `id` | 204 |
| GET | `/api/v1/friendships/pending` | 获取收到的申请 | — | `Friendship[]` |
| GET | `/api/v1/friendships/sent` | 获取已发送的申请 | — | `Friendship[]` |
| GET | `/api/v1/friendships/is-friend/{friendId}` | 检查是否为好友 | Path: `friendId` | `boolean` |
| POST | `/api/v1/friendships/blacklist` | 添加黑名单 | `CreateBlacklistDto` | `Blacklist` (201) |
| GET | `/api/v1/friendships/blacklist` | 获取黑名单 | — | `Blacklist[]` |
| DELETE | `/api/v1/friendships/blacklist/{id}` | 移除黑名单 | Path: `id` | 204 |

#### 群组模块（`GroupsController`）— 需要认证

| 方法 | 路径 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------------|--------|
| GET | `/api/v1/groups` | 获取所有群组 | Query: `page`, `pageSize` | `Group[]` |
| GET | `/api/v1/groups/{id}` | 获取群组详情 | Path: `id` | `Group` |
| GET | `/api/v1/groups/user/{userId}` | 获取用户群组 | Path: `userId` | `Group[]` |
| POST | `/api/v1/groups` | 创建群组 | `CreateGroupDto` | `Group` (201) |
| PUT | `/api/v1/groups/{id}` | 更新群组 | `UpdateGroupDto` | `Group` |
| DELETE | `/api/v1/groups/{id}` | 删除群组 | Path: `id` | 204 |
| POST | `/api/v1/groups/{id}/join` | 加入群组 | Path: `id` | `"成功加入群组"` |
| POST | `/api/v1/groups/{id}/leave` | 退出群组 | Path: `id` | `"成功退出群组"` |
| POST | `/api/v1/groups/{id}/members` | 添加成员 | `AddGroupMemberDto` | `"成功添加成员"` |
| DELETE | `/api/v1/groups/{id}/members/{memberId}` | 移除成员 | Path 参数 | `"成功移除成员"` |
| PUT | `/api/v1/groups/{id}/members/{memberId}` | 更新成员 | `UpdateGroupMemberDto` | `"成功更新成员信息"` |
| GET | `/api/v1/groups/{id}/members` | 获取群成员 | Path: `id` | `GroupMember[]` |

#### 文件模块（`FilesController`）— 需要认证

| 方法 | 路径 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------------|--------|
| POST | `/api/v1/files/upload` | 上传单文件 | `multipart/form-data` (file) | `FileUploadResult` |
| POST | `/api/v1/files/upload-multiple` | 批量上传 | `multipart/form-data` (files) | `FileUploadResult[]` |
| GET | `/api/v1/files/{fileName}` | 获取文件 | Path: `fileName` | 文件二进制流 |
| DELETE | `/api/v1/files/{fileName}` | 删除文件 | Path: `fileName` | 204 |

> **文件限制**：单文件最大 10MB，支持格式：`.jpg` `.jpeg` `.png` `.gif` `.mp4` `.avi` `.mov` `.pdf` `.doc` `.docx` `.txt`

#### Agent 模块（`AgentController`）— 需要认证

| 方法 | 路径 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------------|--------|
| POST | `/api/v1/agent/ask` | RAG 智能问答 | `AgentAskDto { question, knowledgeBaseId? }` | `AgentResponse { answer, sources[] }` |

#### 知识库模块（`KnowledgeController`）— 需要认证

| 方法 | 路径 | 说明 | 请求体/参数 | 响应体 |
|------|------|------|------------|--------|
| POST | `/api/v1/knowledge/bases` | 创建知识库 | `CreateKnowledgeBaseDto` | `KnowledgeBase` (201) |
| GET | `/api/v1/knowledge/bases` | 获取所有知识库 | — | `KnowledgeBase[]` |
| GET | `/api/v1/knowledge/bases/{id}` | 获取知识库详情 | Path: `id` | `KnowledgeBase` |
| PUT | `/api/v1/knowledge/bases/{id}` | 更新知识库 | `UpdateKnowledgeBaseDto` | `KnowledgeBase` |
| DELETE | `/api/v1/knowledge/bases/{id}` | 删除知识库 | Path: `id` | 204 |
| POST | `/api/v1/knowledge/bases/{knowledgeBaseId}/documents/upload` | 上传文档 | `multipart/form-data` (file) | `Knowledge` (201) |
| GET | `/api/v1/knowledge/documents/{knowledgeId}/status` | 查询文档处理状态 | Path: `knowledgeId` | `Knowledge`（含 ParseStatus） |
| POST | `/api/v1/knowledge/bases/{knowledgeBaseId}/chunks` | 添加单个分块 | `CreateKnowledgeChunkDto` | `KnowledgeChunk` (201) |
| POST | `/api/v1/knowledge/bases/{knowledgeBaseId}/chunks/batch` | 批量添加分块 | `CreateKnowledgeChunkDto[]` | `KnowledgeChunk[]` (201) |
| GET | `/api/v1/knowledge/bases/{knowledgeBaseId}/chunks` | 获取分块列表 | Query: `page`, `pageSize` | `KnowledgeChunk[]` |
| DELETE | `/api/v1/knowledge/bases/{knowledgeBaseId}/chunks/{chunkId}` | 删除分块 | Path 参数 | 204 |
| DELETE | `/api/v1/knowledge/bases/{knowledgeBaseId}/chunks` | 清空所有分块 | Path: `knowledgeBaseId` | 204 |

---

### 5.3 HTTP 错误码规范

| 状态码 | 含义 | 前端处理 |
|--------|------|---------|
| 200 | 成功 | 正常处理响应数据 |
| 201 | 创建成功 | 正常处理（POST 创建资源） |
| 204 | 无内容 | 操作成功，无返回体（DELETE） |
| 400 | 请求错误 | 显示后端返回的业务错误消息 |
| 401 | 未授权 | 显示错误消息，自动触发登出 |
| 403 | 禁止访问 | 显示"拒绝访问" |
| 404 | 资源不存在 | 显示"请求的资源不存在" |
| 500 | 服务器错误 | 显示后端错误消息或默认提示 |

---

## 六、SignalR 实时通信规范

### 6.1 连接配置

| 配置项 | 值 | 说明 |
|-------|-----|------|
| Hub 路径 | `/chatHub` | WebSocket 连接端点 |
| 认证方式 | JWT Token | 通过 `accessTokenFactory` 传递 |
| 自动重连 | 是 | 重连间隔：1s → 2s → 5s → 10s → 30s |
| 日志级别 | Information | SignalR 客户端日志 |

**连接生命周期**：

```
App.vue onMounted
  → authStore.initUser()              // 恢复用户信息
  → signalrStore.initConnection()     // 建立 WebSocket 连接
    → useSignalR().startConnection()
      → new HubConnectionBuilder()
          .withUrl('/chatHub', { accessTokenFactory })
      → connection.start()
    → signalRAdapter.attach(connection)  // 注册事件转发
```

---

### 6.2 事件转发架构

前端采用**三层解耦架构**处理 SignalR 事件：

```
SignalR Connection (WebSocket)
    │
    │  conn.on(eventName, handler)
    ▼
SignalRAdapter (src/realtime/adapter.ts)
      │  将 Hub 事件 1:1 转发到事件总线
    │
    │  realtimeBus.emit(eventName, data)
    ▼
RealtimeBus (src/realtime/bus.ts)
      │  发布/订阅事件总线，与 SignalR 完全解耦
      │
      │  handler(data)
    ▼
Handlers (src/realtime/handlers.ts)
      │  应用启动时注册一次，调用 Pinia Store 方法
    ▼
Pinia Stores (chat / friendship / ...)
      │  更新响应式状态
    ▼
Vue 组件自动响应式更新 UI
```

**设计优势**：
- SignalR 连接层与业务逻辑完全解耦
- 事件总线可独立测试
- 易于替换底层通信协议

---

### 6.3 服务端推送事件（Server → Client）

#### 好友相关事件

| 事件名 | 触发时机 | 数据结构 | 前端处理 |
|--------|---------|---------|---------|
| `PendingFriendRequestsLoaded` | 用户连接时 | `Friendship[]` | `friendshipStore.setPendingRequests(data)` |
| `FriendRequestReceived` | 收到好友申请 | `Friendship` | `friendshipStore.appendPendingRequest(data)` + 提示 |
| `FriendRequestAccepted` | 申请被接受 | `{ friendshipId, friendName }` | `friendshipStore.removeSentRequest()` + 提示 |
| `FriendRequestRejected` | 申请被拒绝 | `{ friendshipId, friendName }` | `friendshipStore.removeSentRequest()` + 提示 |
| `NewFriendAdded` | 新好友添加 | `Friendship` | `friendshipStore.appendFriend(data)` |

#### 消息相关事件

| 事件名 | 触发时机 | 数据结构 | 前端处理 |
|--------|---------|---------|---------|
| `ReceiveMessage` | 收到私聊消息 | `Message`（完整 DTO） | `chatStore.addMessage()` + `updateSessionLastMessage()` |
| `ReceiveGroupMessage` | 收到群聊消息 | `Message`（完整 DTO） | `chatStore.addMessage()` + `updateSessionLastMessage()` |
| `MessageRead` | 消息被已读 | `{ messageId, readBy, timestamp }` | 标记失效刷新 |
| `MessageDelivered` | 消息已送达 | `{ messageId, deliveredTo, timestamp }` | 标记失效刷新 |

#### 在线状态事件

| 事件名 | 触发时机 | 数据结构 | 前端处理 |
|--------|---------|---------|---------|
| `FriendsOnlineStatusLoaded` | 用户连接时（批量） | `[{ userId, isOnline, lastSeen }]` | `chatStore.batchUpdateOnlineStatus()` |
| `UserStatusChanged` | 好友上/下线或手动切换 | `{ userId, isOnline, timestamp }` | `chatStore.updateSessionOnlineStatus()` |

---

### 6.4 客户端调用方法（Client → Server）

| Hub 方法 | 说明 | 参数 | 调用位置 |
|----------|------|------|---------|
| `JoinGroup(groupId)` | 加入群组房间 | `string` | `useSignalR().joinGroup()` |
| `LeaveGroup(groupId)` | 离开群组房间 | `string` | `useSignalR().leaveGroup()` |
| `SendMessageToUser(targetUserId, message)` | 发消息给用户 | `string, string` | `useSignalR().sendMessageToUser()` |
| `SendMessageToGroup(groupId, message)` | 发消息给群组 | `string, string` | `useSignalR().sendMessageToGroup()` |
| `UpdateOnlineStatus(isOnline)` | 手动切换在线状态（广播范围：好友 ∪ 所有在线用户） | `boolean` | `Chat.vue handleOnlineStatusChange()` |
| `MarkMessageAsRead(messageId, targetUserId)` | 标记消息已读 | `string, string` | `chatStore.markAsRead()` |
| `MarkMessageAsDelivered(messageId, targetUserId)` | 标记消息已送达 | `string, string` | `useSignalR().markMessageAsDelivered()` |

---

### 6.5 SignalR 分组策略

| 分组名格式 | 用途 | 加入时机 | 离开时机 |
|-----------|------|---------|---------|
| `user_{userId}` | 个人消息通道 | `OnConnectedAsync` | `OnDisconnectedAsync` |
| `group_{groupId}` | 群组消息通道 | 客户端调用 `JoinGroup` | 客户端调用 `LeaveGroup` |

### 6.6 在线状态管理机制

#### 多连接计数模型

ChatHub 使用**内存在线用户表**（`ConcurrentDictionary<Guid, HashSet<string>>`）管理用户的多设备/多标签页连接：

```
_onlineUsers: {
  userId_A → { connId_1, connId_2 }    // 用户 A 有 2 个活跃连接
  userId_B → { connId_3 }              // 用户 B 有 1 个活跃连接
}
```

| 设计要点 | 说明 |
|---------|------|
| 数据结构 | `static ConcurrentDictionary<Guid, HashSet<string>>` — 所有 Hub 实例共享 |
| 线程安全 | 对 `HashSet<string>` 的操作通过 `lock(_lock)` 保护 |
| 上线判定 | 第一个连接加入时（`connections.Count == 0 → 1`），判定为上线 |
| 下线判定 | 最后一个连接断开时（`connections.Count == 1 → 0`），判定为下线 |
| 条目清理 | 下线时自动从字典中移除该用户条目（`TryRemove`） |

#### OnConnectedAsync 详细流程

```
WebSocket 连接建立
    │
    ├─ 1. 加入个人 SignalR 组 `user_{userId}`
    │
    ├─ 2. 将 ConnectionId 加入内存在线表
    │     lock → GetOrAdd → 记录 wasOffline → Add(connId)
    │
    ├─ 3. 如果 wasOffline（首次连接）
    │     ├─ 更新数据库 IsOnline = true
    │     └─ 遍历好友列表 → 向每个好友的 `user_{friendId}` 组推送 `UserStatusChanged`
    │
    ├─ 4. 推送好友在线状态列表给当前用户
    │     遍历好友列表 → 从内存查询 IsUserOnline(friendId) → `FriendsOnlineStatusLoaded`
    │     （无需访问数据库，直接从内存字典判断）
    │
    └─ 5. 推送待处理的好友申请 → `PendingFriendRequestsLoaded`
```

#### OnDisconnectedAsync 详细流程

```
WebSocket 连接断开
    │
    ├─ 1. 从内存在线表移除该 ConnectionId
    │     lock → Remove(connId) → 检查 isNowOffline
    │     如果无剩余连接 → TryRemove(userId) 清理条目
    │
    ├─ 2. 如果 isNowOffline（最后一个连接断开）
    │     ├─ 更新数据库 IsOnline = false
    │     └─ 遍历好友列表 → 向每个好友推送 `UserStatusChanged { isOnline: false }`
    │
    └─ 3. 从个人 SignalR 组 `user_{userId}` 移除
```

#### UpdateOnlineStatus 手动切换流程

用户通过 UI 手动切换在线/离线状态时，广播范围为**好友列表 ∪ 所有在线用户**（取并集），确保所有可能看到该用户状态的人都能收到更新：

```
客户端调用 UpdateOnlineStatus(isOnline)
    │
    ├─ 1. 更新数据库 IsOnline = isOnline
    │
    ├─ 2. 收集通知目标（并集）
    │     ├─ 2a. 好友列表（从数据库查询）
    │     └─ 2b. 所有在线用户（从内存字典 _onlineUsers.Keys 获取，O(n)）
    │
    └─ 3. 向并集中的所有用户推送 `UserStatusChanged`
          foreach targetId → Clients.Group(`user_{targetId}`).SendAsync(...)
```

> **设计说明**：手动切换的广播范围比自动上下线更大，因为手动切换是用户的主动行为，需要确保所有相关方都能看到状态变化。自动上下线仅通知好友列表。

#### 静态工具方法

ChatHub 暴露以下静态方法，供 Controller 等外部组件查询在线状态：

| 方法 | 签名 | 说明 |
|------|------|------|
| `OnlineUserCount` | `static int` (属性) | 当前在线用户数（可用于监控/调试） |
| `CheckUserOnline` | `static bool CheckUserOnline(Guid userId)` | 检查指定用户是否在线 |

---

## 七、RAG 知识库系统

### 7.1 系统架构

RAG 知识库系统是微钉的核心 AI 功能，实现了一个完整的检索增强生成流水线。

```
┌─────────────────────────────────────────────────────────────────┐
│                    RAG Knowledge Base System                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────┐│
│  │  Document     │   │  Knowledge   │   │  Agent / Query       ││
│  │  Ingestion    │   │  Storage     │   │  Interface           ││
│  │              │   │              │   │                      ││
│  │ • Upload     │   │ • SQL Server │   │ • POST /agent/ask    ││
│  │ • Parse      │   │   (metadata) │   │ • Query understand   ││
│  │ • Chunk      │   │ • Vector DB  │   │ • Retrieve + Rerank  ││
│  │ • Embed      │   │   (vectors)  │   │ • LLM generate       ││
│  └──────┬───────┘   └──────┬───────┘   └──────────┬───────────┘│
│         │                  │                      │            │
│         ▼                  ▼                      ▼            │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    Infrastructure Layer                       ││
│  │  • OnnxEmbeddingService (BGE-small-zh-v1.5, 512d)          ││
│  │  • OnnxRerankService (bge-reranker-base)                    ││
│  │  • QdrantVectorStore / InMemoryVectorStore (可配置切换)      ││
│  │  • DocumentParser (txt/md/pdf/docx/code)                    ││
│  │  • BertTokenizer (WordPiece, Chinese support)               ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    External Services                          ││
│  │  • DeepSeek API (LLM generation)                            ││
│  │  • ONNX Runtime (local model inference)                     ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 RAG 流水线

`RagAgentEngine` 实现了 6 阶段流水线：

#### 阶段 ① 查询理解（Query Understanding）

**服务**：`QueryUnderstandingService`

- 使用 LLM 对用户原始查询进行**意图分类**：`KnowledgeSearch`（需要检索知识库）或 `DirectChat`（直接对话）
- 对模糊查询进行**查询改写**，生成更适合向量检索的查询文本
- 失败时优雅降级，使用原始查询继续

#### 阶段 ② 意图路由（Intent Routing）

- `DirectChat`：跳过检索，直接将问题发送给 LLM 生成回答
- `KnowledgeSearch`：进入向量检索流程

#### 阶段 ③ 向量检索（Vector Retrieval）

**服务**：`RetrievalService`

- 使用 `OnnxEmbeddingService` 将查询文本编码为 512 维向量
- 在 `InMemoryVectorStore` 中执行余弦相似度搜索
- 召回 3x TopK 候选文档（过召回以供重排筛选）

#### 阶段 ④ 交叉编码重排（Reranking）

**服务**：`OnnxRerankService`

- 使用 `bge-reranker-base` 交叉编码模型对每个 (查询, 文档) 对重新评分
- 交叉编码器同时处理查询和文档的 token，比双编码器（嵌入模型）精度更高
- 输出 TopK 个最终结果

#### 阶段 ⑤ 上下文组装（Context Assembly）

- 将 TopK 知识分块组装为带编号的上下文文本
- 保留分块的来源信息（文档名、分块索引等）

#### 阶段 ⑥ LLM 生成（Generation）

**服务**：`DeepSeekLlmService`

- 通过 Semantic Kernel 的 OpenAI 兼容接口调用 DeepSeek API
- 支持思维/推理模式（thinking mode）切换
- 可配置推理努力级别（reasoning effort）
- 返回回答文本及引用来源

---

### 7.3 文档摄取管道

文档上传后的异步处理流程：

```
文件上传 (multipart/form-data)
    │
    ▼
创建 Knowledge 记录 (ParseStatus = Pending)
    │
    ▼
异步处理（System.Threading.Channels 生产者-消费者）
    │
    ├─ ① DocumentParser 解析文件
    │     • .txt / .md → 直接读取文本
    │     • .pdf → PdfPig 提取文本
    │     • .docx → OpenXml 提取文本
    │     • 代码/配置文件 → 按格式读取
    │
    ├─ ② TextChunkingService 文本分块
    │     • 段落分割（按换行符）
    │     • 合并小段至目标大小（默认 chunk size）
    │     • 超长段按句子边界切分
    │     • 添加重叠窗口（overlap）
    │
    ├─ ③ OnnxEmbeddingService 向量化
    │     • BERT WordPiece 分词
    │     • ONNX 模型推理
    │     • 均值池化 + L2 归一化
    │     • 输出 512 维向量
    │
    ├─ ④ 存储
    │     • KnowledgeChunk → SQL Server（文本、元数据）
    │     • Vector → InMemoryVectorStore（向量索引）
    │
    └─ ⑤ 更新状态
          • ParseStatus → Completed
          • 更新 KnowledgeBase.ChunkCount
```

**支持的文件格式**：

| 类别 | 格式 |
|------|------|
| 文本 | `.txt`, `.md` |
| 文档 | `.pdf`（PdfPig）, `.docx`（OpenXml） |
| 代码 | `.cs`, `.java`, `.py`, `.js`, `.ts`, `.go`, `.rs`, `.cpp`, `.c`, `.h`, `.hpp` |
| 配置 | `.json`, `.xml`, `.yaml`, `.yml`, `.toml`, `.ini`, `.env` |
| 标记 | `.html`, `.css`, `.scss`, `.less` |
| 脚本 | `.sh`, `.bat`, `.ps1` |
| 其他 | `.sql`, `.dockerfile`, `.makefile` |

---

### 7.4 向量存储与检索

#### QdrantVectorStore（生产环境）

基于 `Qdrant.Client` gRPC 实现 Semantic Kernel 的 `VectorStore` 抽象类：

| 特性 | 说明 |
|------|------|
| 存储方式 | Qdrant 向量数据库（gRPC 连接） |
| 搜索算法 | HNSW 近似最近邻搜索 |
| 维度 | 512（BGE-small-zh-v1.5 输出） |
| 距离函数 | 余弦相似度（Cosine Similarity） |
| 持久化 | 数据持久化到 Qdrant 存储卷 |
| 索引 | 支持 payload 字段索引（`IsIndexed = true`） |
| 切换方式 | `appsettings.json` 中 `VectorStore:Provider` 设为 `Qdrant` |

#### InMemoryVectorStore（开发环境）

自定义实现 Semantic Kernel 的 `VectorStore` 抽象类，作为开发阶段的轻量替代：

| 特性 | 说明 |
|------|------|
| 存储方式 | 内存字典（`ConcurrentDictionary`） |
| 搜索算法 | 余弦相似度暴力搜索 |
| 维度 | 512（BGE-small-zh-v1.5 输出） |
| 持久化 | 无（进程重启后数据丢失） |
| 切换方式 | `appsettings.json` 中 `VectorStore:Provider` 设为 `InMemory`（默认） |

**配置切换**：两个实现共享同一个 `VectorStore` 抽象接口，`RetrievalService` 等上层代码零改动。通过 DI 注册和配置文件切换：

```json
// appsettings.json
"VectorStore": {
    "Provider": "Qdrant",          // "InMemory" 或 "Qdrant"
    "Qdrant": {
        "Host": "localhost",
        "Port": 6334               // gRPC 端口
    }
}
```

#### OnnxEmbeddingService

| 特性 | 说明 |
|------|------|
| 模型 | BGE-small-zh-v1.5（ONNX 格式） |
| 分词器 | 手写 BERT WordPiece（支持中文字符级处理） |
| 池化策略 | 均值池化（Mean Pooling） |
| 归一化 | L2 归一化 |
| 输出维度 | 512 |
| 模型加载 | 支持本地路径或 URL 自动下载 |

#### OnnxRerankService

| 特性 | 说明 |
|------|------|
| 模型 | bge-reranker-base（ONNX 格式） |
| 工作方式 | 交叉编码：同时编码查询和文档 token |
| 输入 | (query, document) 对 |
| 输出 | 相关性得分 |
| 用途 | 从 3x TopK 候选中筛选出 TopK 最终结果 |

---

### 7.5 LLM 集成

#### DeepSeekLlmService

通过 Microsoft Semantic Kernel 的 OpenAI 兼容接口调用 DeepSeek API：

| 配置项 | 说明 |
|--------|------|
| 提供商 | DeepSeek API |
| 模型 | `deepseek-v4-pro` |
| 基础 URL | `https://api.deepseek.com` |
| 思维模式 | 支持 thinking/reasoning 模式切换 |
| 推理努力 | 可配置推理努力级别 |
| 可替换性 | 任何 OpenAI 兼容 API 均可替换 |

#### 配置示例（`appsettings.json`）

```json
{
  "DeepSeek": {
    "ApiKey": "your-api-key",
    "ModelId": "deepseek-v4-pro",
    "BaseUrl": "https://api.deepseek.com",
    "ThinkingEnabled": false,
    "ReasoningEffort": "high"
  },
  "VectorSearch": {
    "ModelPath": "Models",
    "TopK": 5,
    "EnableRAG": true
  },
  "Rerank": {
    "ModelPath": "Models",
    "ModelName": "bge-reranker-base.onnx",
    "TopK": 5,
    "EnableRerank": true
  },
  "Ingestion": {
    "ChunkSize": 500,
    "ChunkOverlap": 50,
    "BatchSize": 10,
    "UploadDir": "wwwroot/uploads/knowledge"
  },
  "VectorStore": {
    "Provider": "Qdrant",
    "Qdrant": {
      "Host": "localhost",
      "Port": 6334
    }
  }
}
```

---

## 八、核心数据流时序

### 8.1 发送消息完整流程

```
发送方浏览器                      后端服务                        接收方浏览器
    │                              │                                │
    │── POST /api/v1/messages ────>│                                │
    │   { relationId, content,     │                                │
    │     messageType }            │                                │
    │                              │── MessageService               │
    │                              │     .SendMessageAsync()        │
    │                              │   ├─ 写入动态分表               │
    │                              │   │  Message_{relationId}      │
    │                              │   └─ 补全 SenderName/Avatar   │
    │                              │                                │
    │                              │── 查询 Friendship              │
    │                              │   确定接收方                    │
    │                              │   回填 ReceiverId/ReceiverName │
    │                              │                                │
    │                              │── hubContext.SendAsync          │
    │                              │   "ReceiveMessage" ───────────>│
    │                              │   (完整 MessageDto)             │
    │                              │                                │
    │<── 201 + MessageDto ─────────│                                │── signalRAdapter 转发
    │                              │                                │── realtimeBus.emit
    │── chatStore.addMessage()     │                                │── handlers.ts 处理
    │── updateSessionLastMessage() │                                │── chatStore.addMessage()
    │── scrollToBottom()           │                                │── updateSessionLastMessage()
    │── UI 更新                     │                                │── UI 更新
```

### 8.2 在线状态同步流程（多连接模型）

```
用户 A 第 1 个连接              ChatHub（内存在线表）            用户 B（好友）
    │                            │                               │
    │── WebSocket 连接 ─────────>│                               │
    │                            │── _onlineUsers[A] = {conn1}   │
    │                            │   wasOffline = true            │
    │                            │── DB: IsOnline = true          │
    │                            │                                │
    │                            │── "UserStatusChanged" ────────>│
    │                            │   { userId:A, isOnline:true }  │
    │                            │                                │── updateSessionOnlineStatus
    │                            │                                │── UI: 显示"在线"
    │                            │                                │
    │<── "FriendsOnlineStatusLoaded" ──│                          │
    │   [{ userId:B, isOnline:   │     │                          │
    │     IsUserOnline(B) }]     │     │  ← 从内存查询，非数据库   │
    │── batchUpdateOnlineStatus()      │                          │

用户 A 第 2 个连接（新标签页）
    │                            │                                │
    │── WebSocket 连接 ─────────>│                                │
    │                            │── _onlineUsers[A] = {conn1,conn2}
    │                            │   wasOffline = false           │
    │                            │   （不触发上线广播）              │
    │                            │                                │
    │<── "FriendsOnlineStatusLoaded" ──│                          │

用户 A 关闭第 1 个标签页
    │                            │                                │
    │── WebSocket 断开 ─────────>│                                │
    │                            │── _onlineUsers[A] = {conn2}   │
    │                            │   isNowOffline = false         │
    │                            │   （不触发下线广播）              │

用户 A 关闭最后一个标签页
    │                            │                                │
    │── WebSocket 断开 ─────────>│                                │
    │                            │── _onlineUsers.Remove(A)      │
    │                            │   isNowOffline = true          │
    │                            │── DB: IsOnline = false         │
    │                            │                                │
    │                            │── "UserStatusChanged" ────────>│
    │                            │   { userId:A, isOnline:false } │
    │                            │                                │── updateSessionOnlineStatus
    │                            │                                │── UI: 显示"离线"
```

**多连接核心规则**：
- 第一个连接建立时触发上线广播，中间的连接增减不触发任何广播
- 最后一个连接断开时触发下线广播
- 好友在线状态查询从内存字典直接判断，无需访问数据库

**时序竞态处理**：`FriendsOnlineStatusLoaded` 可能在 `Chat.vue` 的 sessions 创建之前到达。解决方案：

- `chatStore` 维护 `pendingOnlineStatuses` 缓存
- `updateSessionOnlineStatus` 始终先写缓存，再更新已有 sessions
- `addSession` 创建新会话时自动从缓存读取在线状态

### 8.3 好友申请流程

```
用户 A                         后端                          用户 B
  │                              │                              │
  │── POST /friendships ────────>│                              │
  │   { friendId: B }            │                              │
  │                              │── 创建 Friendship            │
  │                              │   (IsActive = false)         │
  │                              │── hubContext.SendAsync        │
  │                              │   "FriendRequestReceived" ──>│
  │<── 201 + Friendship ─────────│                              │── 显示好友申请通知
  │                              │                              │
  │                              │ （用户 B 接受申请）            │
  │                              │<── POST /friendships/{id}/accept
  │                              │                              │
  │                              │── 激活双向 Friendship         │
  │                              │── 创建 Conversation + 分表    │
  │                              │                              │
  │   "FriendRequestAccepted" <──│                              │
  │   { friendshipId, friendName }│                             │
  │                              │── "NewFriendAdded" ─────────>│
  │<── "NewFriendAdded" ─────────│                              │
```

### 8.4 手动切换在线状态流程

```
用户 A（手动切换"离线"）        ChatHub                       用户 B（好友） + 其他在线用户
    │                            │                               │
    │── invoke('UpdateOnlineStatus', false) ─>│                  │
    │                            │                               │
    │                            │── DB: IsOnline = false         │
    │                            │                               │
    │                            │── 收集通知目标                  │
    │                            │   好友列表 ∪ _onlineUsers.Keys │
    │                            │   （去除自身后取并集）            │
    │                            │                               │
    │                            │── "UserStatusChanged" ───────>│（所有目标用户）
    │                            │   { userId:A, isOnline:false } │
    │                            │                               │── updateSessionOnlineStatus
    │                            │                               │── UI: 显示"离线"
```

### 8.5 RAG 问答流程

```
用户                        前端                          后端                         AI 服务
 │                            │                            │                            │
 │── 输入问题 ───────────────>│                            │                            │
 │                            │── POST /agent/ask ────────>│                            │
 │                            │   { question, kbId? }      │                            │
 │                            │                            │── RagAgentEngine           │
 │                            │                            │                            │
 │                            │                            │── ① 查询理解 ─────────────>│
 │                            │                            │   LLM 意图分类 + 改写       │
 │                            │                            │<── 意图 + 改写查询 ────────│
 │                            │                            │                            │
 │                            │                            │── ② 向量检索               │
 │                            │                            │   ONNX 嵌入 → 向量搜索      │
 │                            │                            │   → 3x TopK 候选           │
 │                            │                            │                            │
 │                            │                            │── ③ 重排 ─────────────────>│
 │                            │                            │   bge-reranker-base 精排    │
 │                            │                            │<── TopK 结果 ──────────────│
 │                            │                            │                            │
 │                            │                            │── ④ 上下文组装             │
 │                            │                            │   编号知识片段 + 问题       │
 │                            │                            │                            │
 │                            │                            │── ⑤ LLM 生成 ────────────>│
 │                            │                            │   DeepSeek API              │
 │                            │                            │<── 生成回答 ───────────────│
 │                            │                            │                            │
 │                            │<── { answer, sources[] } ──│                            │
 │<── 显示回答 + 引用来源 ─────│                            │                            │
```

---

## 九、数据类型定义（前后端对照）

### 9.1 核心实体类型

| 前端类型 (TypeScript) | 后端 DTO (C#) | 说明 |
|----------------------|--------------|------|
| `User` | `UserDto` | 用户信息 |
| `Message` | `MessageDto` | 消息（含 relationId、发送者信息、附件、状态） |
| `MessageAttachment` | `MessageAttachmentDto` | 消息附件 |
| `MessageStatus` | `MessageStatusDto` | 消息状态（Sent/Delivered/Read） |
| `Friendship` | `FriendshipDto` | 好友关系（含 conversationId） |
| `Group` | `GroupDto` | 群组信息 |
| `GroupMember` | `GroupMemberDto` | 群组成员 |
| `Blacklist` | `BlacklistDto` | 黑名单 |
| `FileUploadResult` | `FileUploadResult` | 文件上传结果 |
| `KnowledgeBase` | `KnowledgeBaseDto` | 知识库（名称、描述、索引策略、分块数量） |
| `Knowledge` | `KnowledgeDto` | 知识文档（文件信息、解析状态） |
| `KnowledgeChunk` | `KnowledgeChunkDto` | 知识分块（文本内容、元数据、来源） |

### 9.2 SignalR 事件专用类型

```typescript
// 用户状态变化（单个好友上线/下线）
interface UserStatusChange {
  userId: string
  isOnline: boolean
  timestamp: string    // ISO 8601 UTC
}

// 好友在线状态（连接时批量推送）
interface FriendOnlineStatus {
  userId: string
  isOnline: boolean
  lastSeen: string     // ISO 8601 UTC
}

// 消息已读通知
interface MessageReadNotification {
  messageId: string
  readBy: string       // 读取者用户 ID
  timestamp: string
}

// 消息已送达通知
interface MessageDeliveredNotification {
  messageId: string
  deliveredTo: string  // 送达目标用户 ID
  timestamp: string
}
```

### 9.3 前端会话模型

```typescript
// 聊天会话（前端本地构建，不直接对应后端实体）
interface ChatSession {
  id: string           // 会话标识（私聊 = conversationId，群聊 = groupId）
  relationId: string   // 消息 API 的 relationId（用于查改删发）
  type: 'private' | 'group'
  name: string         // 显示名称
  avatar?: string
  lastMessage?: Message
  unreadCount: number
  isOnline?: boolean   // 仅私聊，由 SignalR 实时更新
  lastSeen?: string    // 仅私聊
  friendId?: string    // 仅私聊，用于关联在线状态
}
```

---

## 十、前端状态管理与通信职责

| Store | 文件 | 通信职责 |
|-------|------|---------|
| `authStore` | `stores/auth.ts` | 登录/注册/登出 HTTP 请求；管理 JWT Token；触发 SignalR 连接初始化 |
| `chatStore` | `stores/chat.ts` | 消息发送/加载 HTTP 请求；维护 sessions 和 messages 状态；处理 SignalR 推送的消息和在线状态 |
| `friendshipStore` | `stores/friendship.ts` | 好友列表/申请 HTTP 请求；处理 SignalR 推送的好友事件 |
| `groupStore` | `stores/group.ts` | 群组状态管理 |
| `signalrStore` | `stores/signalr.ts` | 管理 SignalR 连接生命周期（initConnection / disconnect） |
| `invalidationStore` | `stores/invalidation.ts` | 数据失效标记管理；页面进入时根据标记决定是否重新获取数据 |

**数据流向总结**：

- **用户主动操作** → Vue 组件 → Pinia Store → API 模块 → HTTP → Controller → Service → Repository → 数据库
- **实时推送** → ChatHub → SignalR WebSocket → signalRAdapter → realtimeBus → handlers.ts → Pinia Store → Vue 组件响应式更新
- **RAG 问答** → Vue 组件 → POST /api/v1/agent/ask → AgentController → RagAgentEngine → (查询理解 → 检索 → 重排 → LLM 生成) → 返回结果

---

## 十一、开发规范

### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 项目命名 | `WeiDin.{Layer}` | `WeiDin.Core`、`WeiDin.API` |
| 类命名 | PascalCase | `UserService`、`ChatHub` |
| 方法命名 | PascalCase | `GetUserById`、`SendMessageAsync` |
| 接口命名 | `I` 前缀 + PascalCase | `IUserService`、`IRepository<T>` |
| 实体命名 | PascalCase，单数形式 | `User`、`Message`、`Conversation` |
| 前端组件 | PascalCase | `Chat.vue`、`Friends.vue` |
| 前端变量/函数 | camelCase | `chatStore`、`sendMessage` |

### 代码组织

| 类型 | 原则 |
|------|------|
| Controllers | 每个功能模块一个控制器 |
| Services | 每个业务领域一个服务类 |
| Entities | 每个数据库表对应一个实体类 |
| DTOs | 每个 API 端点对应一组 DTO 类 |
| Stores | 每个业务领域一个 Pinia Store |
| Views | 每个页面一个 Vue 组件 |

### 依赖原则

| 原则 | 说明 |
|------|------|
| 上层依赖下层 | API → Application → Infrastructure → Core |
| 不跨层依赖 | Application 不直接依赖 Infrastructure |
| 接口隔离 | 通过接口解耦，便于测试和替换 |
| 单向数据流 | 前端采用 Pinia 单向数据流 + 事件驱动 |
