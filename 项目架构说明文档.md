# 微钉即时通讯系统 - 项目架构说明文档

## 项目概述

微钉即时通讯系统是一个基于 ASP.NET Core 8.0 和 Vue.js 3 开发的现代化即时通讯平台。项目采用领域驱动设计（DDD）和分层架构模式，使用 ABP Framework 作为基础框架，实现了完整的用户管理、消息发送、群组管理、好友管理等功能。

## 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    WeiDin.Frontend                      │
│  (Vue.js 3 + Element Plus + Pinia + SignalR Client)    │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/WebSocket
┌────────────────────▼────────────────────────────────────┐
│                    WeiDin.API                            │
│  (ASP.NET Core 8.0 + SignalR + JWT + Swagger)          │
└────┬───────────────┬───────────────┬────────────────────┘
     │               │               │
┌────▼────┐   ┌──────▼──────┐   ┌───▼────────────┐
│Application│   │Infrastructure│   │     Core      │
│   Layer   │   │    Layer     │   │     Layer     │
└───────────┘   └──────────────┘   └───────────────┘
```

## 模块详细说明

### 1. WeiDin.API（API 层）

**职责**：作为系统的入口层，负责处理 HTTP 请求、WebSocket 连接和系统配置。

**主要功能**：
- RESTful API 端点提供
- SignalR Hub 实时通信
- JWT 身份认证与授权
- CORS 跨域配置
- Swagger API 文档
- 请求管道配置
- 日志记录（Serilog）

**关键文件**：
- `Program.cs` - 应用程序入口，配置 Serilog 和 ABP 框架
- `WeiDinApiModule.cs` - API 模块配置，包含 JWT、CORS、Redis、SignalR 等配置
- `Controllers/` - 包含所有 API 控制器：
  - `AuthController.cs` - 用户认证（注册、登录、登出）
  - `UsersController.cs` - 用户管理
  - `MessagesController.cs` - 消息管理
  - `GroupsController.cs` - 群组管理
  - `FriendshipsController.cs` - 好友管理
  - `FilesController.cs` - 文件上传下载
- `Hubs/ChatHub.cs` - SignalR Hub，处理实时消息推送

**技术栈**：
- ASP.NET Core 8.0
- ABP Framework 8.1.5
- SignalR 1.1.0
- JWT Bearer Authentication
- Swagger/OpenAPI
- Serilog
- Redis 缓存

**依赖关系**：
- 依赖 `WeiDin.Application` 模块（应用服务层）
- 依赖 `WeiDin.Infrastructure` 模块（基础设施层）

---

### 2. WeiDin.Application（应用层）

**职责**：实现业务逻辑，协调领域对象完成业务用例。

**主要功能**：
- 业务逻辑实现
- DTO（数据传输对象）定义
- 服务接口定义
- AutoMapper 对象映射
- 业务验证（FluentValidation）

**关键文件**：
- `WeiDinApplicationModule.cs` - 应用模块配置，注册 AutoMapper
- `Services/` - 应用服务实现：
  - `UserService.cs` - 用户业务逻辑
  - `MessageService.cs` - 消息业务逻辑
  - `GroupService.cs` - 群组业务逻辑
  - `FriendshipService.cs` - 好友关系业务逻辑
- `DTOs/` - 数据传输对象定义
- `Interfaces/` - 服务接口定义
- `Mappings/` - AutoMapper 映射配置

**技术栈**：
- ABP Framework DDD Application 模块
- AutoMapper 12.0.1
- FluentValidation 11.8.1

**依赖关系**：
- 依赖 `WeiDin.Core` 模块（领域实体和接口）
- 不依赖 `WeiDin.Infrastructure`（通过接口解耦）

---

### 3. WeiDin.Core（核心层/领域层）

**职责**：定义领域实体、值对象和核心接口，是系统的核心业务模型。

**主要功能**：
- 领域实体定义
- 核心接口定义
- 领域模型设计

**关键文件**：
- `WeiDinCoreModule.cs` - 核心模块配置（通常无需特殊配置）
- `Entities/` - 领域实体：
  - `User.cs` - 用户实体
  - `Message.cs` - 消息实体
  - `MessageStatus.cs` - 消息状态实体
  - `MessageAttachment.cs` - 消息附件实体
  - `Group.cs` - 群组实体
  - `GroupMember.cs` - 群组成员实体
  - `Friendship.cs` - 好友关系实体
  - `Blacklist.cs` - 黑名单实体
- `Interfaces/` - 核心接口定义

**技术栈**：
- ABP Framework DDD Domain 模块
- Entity Framework Core（仅用于实体定义）

**依赖关系**：
- 不依赖任何其他模块（最底层，被其他模块依赖）

---

### 4. WeiDin.Infrastructure（基础设施层）

**职责**：提供技术实现，包括数据访问、外部服务集成等。

**主要功能**：
- Entity Framework Core 数据上下文配置
- 数据库连接配置
- 仓储模式实现
- 外部服务集成（如 Redis、第三方 API）

**关键文件**：
- `WeiDinInfrastructureModule.cs` - 基础设施模块配置，配置 EF Core 和数据库连接
- `Data/WeiDinDbContext.cs` - EF Core 数据上下文
- `Repositories/` - 仓储实现（如果需要自定义仓储）

**技术栈**：
- Entity Framework Core 8.0.8
- SQL Server
- ABP Framework EF Core 模块
- StackExchange.Redis（用于 Redis 连接）

**依赖关系**：
- 依赖 `WeiDin.Core` 模块（使用领域实体）

---

### 5. WeiDin.Frontend（前端层）

**职责**：提供用户界面，与后端 API 和 SignalR Hub 进行交互。

**主要功能**：
- 用户界面展示
- 用户交互处理
- API 调用
- SignalR 实时通信客户端
- 状态管理
- 路由管理

**关键文件**：
- `package.json` - 项目依赖配置
- `vite.config.ts` - Vite 构建配置
- `src/main.ts` - 应用入口
- `src/App.vue` - 根组件
- `src/router/index.ts` - 路由配置
- `src/stores/` - Pinia 状态管理：
  - `auth.ts` - 认证状态
  - `chat.ts` - 聊天状态
- `src/api/` - API 客户端：
  - `request.ts` - Axios 请求封装
  - `auth.ts` - 认证 API
  - `message.ts` - 消息 API
  - `group.ts` - 群组 API
  - `friendship.ts` - 好友 API
  - `file.ts` - 文件 API
- `src/utils/signalr.ts` - SignalR 客户端封装
- `src/views/` - 页面组件：
  - `Login.vue` - 登录页
  - `Register.vue` - 注册页
  - `Chat.vue` - 聊天页
  - `Friends.vue` - 好友页
  - `Groups.vue` - 群组页
  - `Profile.vue` - 个人资料页
  - `Settings.vue` - 设置页

**技术栈**：
- Vue.js 3.4.0
- TypeScript 5.3.0
- Element Plus 2.4.4
- Pinia 2.1.7
- Vue Router 4.2.5
- Axios 1.6.2
- @microsoft/signalr 8.0.0
- Vite 5.0.10
- Day.js 1.11.10

**依赖关系**：
- 通过 HTTP 和 WebSocket 与 `WeiDin.API` 通信

---

## 模块依赖关系

```
WeiDin.Frontend
    │
    │ (HTTP/WebSocket)
    │
WeiDin.API
    │
    ├─────────────┐
    │             │
WeiDin.Application  WeiDin.Infrastructure
    │             │
    └─────┬───────┘
          │
    WeiDin.Core
```

**依赖说明**：
1. **WeiDin.API** 依赖 `WeiDin.Application` 和 `WeiDin.Infrastructure`
2. **WeiDin.Application** 依赖 `WeiDin.Core`
3. **WeiDin.Infrastructure** 依赖 `WeiDin.Core`
4. **WeiDin.Core** 不依赖任何其他模块（最底层）
5. **WeiDin.Frontend** 通过 HTTP/WebSocket 与 `WeiDin.API` 通信

---

## 数据流向

### 请求流程

```
用户操作 (Frontend)
    ↓
HTTP 请求 / WebSocket 连接
    ↓
WeiDin.API (Controllers/Hubs)
    ↓
WeiDin.Application (Services)
    ↓
WeiDin.Infrastructure (Repositories)
    ↓
数据库 (SQL Server)
```

### 响应流程

```
数据库 (SQL Server)
    ↓
WeiDin.Infrastructure (Repositories)
    ↓
WeiDin.Application (Services + DTOs)
    ↓
WeiDin.API (Controllers/Hubs)
    ↓
HTTP 响应 / WebSocket 推送
    ↓
WeiDin.Frontend (UI 更新)
```

---

## 核心功能模块

### 1. 用户认证模块
- **位置**：`WeiDin.API/Controllers/AuthController.cs`
- **服务**：`WeiDin.Application/Services/UserService.cs`
- **实体**：`WeiDin.Core/Entities/User.cs`
- **功能**：用户注册、登录、JWT Token 生成、密码加密

### 2. 消息模块
- **位置**：`WeiDin.API/Controllers/MessagesController.cs` + `Hubs/ChatHub.cs`
- **服务**：`WeiDin.Application/Services/MessageService.cs`
- **实体**：`WeiDin.Core/Entities/Message.cs`、`MessageStatus.cs`、`MessageAttachment.cs`
- **功能**：发送消息、接收消息、消息状态管理、文件附件、实时推送

### 3. 群组模块
- **位置**：`WeiDin.API/Controllers/GroupsController.cs`
- **服务**：`WeiDin.Application/Services/GroupService.cs`
- **实体**：`WeiDin.Core/Entities/Group.cs`、`GroupMember.cs`
- **功能**：创建群组、加入/退出群组、群成员管理、群组消息

### 4. 好友模块
- **位置**：`WeiDin.API/Controllers/FriendshipsController.cs`
- **服务**：`WeiDin.Application/Services/FriendshipService.cs`
- **实体**：`WeiDin.Core/Entities/Friendship.cs`、`Blacklist.cs`
- **功能**：添加好友、删除好友、好友列表、黑名单管理

### 5. 文件模块
- **位置**：`WeiDin.API/Controllers/FilesController.cs`
- **功能**：文件上传、文件下载、文件类型验证、文件大小限制

---

## 技术特性

### 后端特性
- ✅ **分层架构**：清晰的分层设计，职责分离
- ✅ **DDD 设计**：领域驱动设计，业务逻辑集中在领域层
- ✅ **依赖注入**：使用 ABP Framework 的 Autofac 容器
- ✅ **JWT 认证**：安全的 Token 认证机制
- ✅ **实时通信**：SignalR 实现双向实时通信
- ✅ **缓存支持**：Redis 缓存提升性能
- ✅ **API 文档**：Swagger 自动生成 API 文档
- ✅ **日志记录**：Serilog 结构化日志
- ✅ **对象映射**：AutoMapper 简化对象转换
- ✅ **数据验证**：FluentValidation 业务规则验证

### 前端特性
- ✅ **现代化框架**：Vue.js 3 Composition API
- ✅ **TypeScript**：类型安全，提升代码质量
- ✅ **组件库**：Element Plus 丰富的 UI 组件
- ✅ **状态管理**：Pinia 轻量级状态管理
- ✅ **路由管理**：Vue Router 单页应用路由
- ✅ **HTTP 客户端**：Axios 统一请求处理
- ✅ **实时通信**：SignalR 客户端实时消息
- ✅ **构建工具**：Vite 快速构建和热更新

---

## 开发规范

### 命名规范
- **项目命名**：`WeiDin.{Layer}` 格式
- **类命名**：PascalCase（如 `UserService`）
- **方法命名**：PascalCase（如 `GetUserById`）
- **接口命名**：以 `I` 开头（如 `IUserService`）
- **实体命名**：PascalCase，单数形式（如 `User`、`Message`）

### 代码组织
- **Controllers**：每个功能模块一个控制器
- **Services**：每个业务领域一个服务类
- **Entities**：每个数据库表对应一个实体类
- **DTOs**：每个 API 端点对应一个 DTO 类

### 依赖原则
- **上层依赖下层**：API → Application → Infrastructure → Core
- **不跨层依赖**：Application 不直接依赖 Infrastructure
- **接口隔离**：通过接口解耦，便于测试和替换

---

## 部署说明

### 后端部署
1. 配置数据库连接字符串（`appsettings.json`）
2. 配置 Redis 连接字符串
3. 配置 JWT 密钥
4. 运行数据库迁移
5. 发布到 IIS 或 Docker 容器

### 前端部署
1. 安装依赖：`npm install`
2. 配置 API 地址（环境变量）
3. 构建项目：`npm run build`
4. 部署到 Nginx 或静态文件服务器

---

## 总结

微钉即时通讯系统采用现代化的分层架构设计，通过清晰的模块划分实现了高内聚、低耦合的系统结构。各模块职责明确，便于维护和扩展。使用 ABP Framework 作为基础框架，大大简化了开发工作，同时保证了代码质量和架构规范。

**核心优势**：
- 🎯 **清晰的架构**：分层明确，易于理解和维护
- 🔒 **安全性**：JWT 认证、密码加密、权限控制
- ⚡ **高性能**：Redis 缓存、SignalR 实时通信
- 📚 **可维护性**：代码规范、文档完善
- 🚀 **可扩展性**：模块化设计，易于扩展新功能

---

*本文档最后更新时间：2025年*

