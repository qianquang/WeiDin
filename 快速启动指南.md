# 微钉即时通讯系统 - 快速启动指南

## 🚀 快速开始

### 方法1：一键启动（推荐）

**Windows用户**：
```bash
# 双击运行
WeiDin.API\Scripts\SetupDatabase.bat
```

**Linux/Mac用户**：
```bash
# 给脚本执行权限
chmod +x WeiDin.API/Scripts/SetupDatabase.sh

# 运行脚本
./WeiDin.API/Scripts/SetupDatabase.sh
```

### 方法2：手动启动

#### 1. 环境准备
- ✅ .NET 8.0 SDK
- ✅ SQL Server LocalDB（或SQL Server Express）
- ✅ Redis（可选，用于缓存）

#### 2. 安装依赖
```bash
dotnet restore
```

#### 3. 构建项目
```bash
dotnet build
```

#### 4. 创建数据库
```bash
# 安装EF Core工具
dotnet tool install --global dotnet-ef

# 创建迁移
dotnet ef migrations add InitialCreate --project WeiDin.API --startup-project WeiDin.API

# 更新数据库
dotnet ef database update --project WeiDin.API --startup-project WeiDin.API
```

#### 5. 启动应用
```bash
dotnet run --project WeiDin.API
```

## 🌐 访问应用

启动成功后，在浏览器中访问：

- **API文档**: https://localhost:7000/swagger
- **HTTPS**: https://localhost:7000
- **HTTP**: http://localhost:5000

## 📋 数据库连接配置

### 默认配置（SQL Server LocalDB）
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=WeiDinDb;Trusted_Connection=true;MultipleActiveResultSets=true;TrustServerCertificate=true",
    "Redis": "localhost:6379"
  }
}
```

### 修改数据库连接

编辑 `WeiDin.API/appsettings.json` 文件：

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "你的数据库连接字符串",
    "Redis": "你的Redis连接字符串"
  }
}
```

## 🔧 常见问题

### 1. 数据库连接失败
- 确保SQL Server LocalDB已安装
- 检查连接字符串是否正确
- 确认数据库服务正在运行

### 2. 端口被占用
- 修改 `launchSettings.json` 中的端口号
- 或停止占用端口的其他程序

### 3. 权限问题
- 以管理员身份运行命令提示符
- 确保有创建数据库的权限

## 📚 API测试

### 1. 用户注册
```bash
POST https://localhost:7000/api/v1/auth/register
Content-Type: application/json

{
  "username": "testuser",
  "email": "test@example.com",
  "phoneNumber": "13800138000",
  "password": "123456",
  "nickname": "测试用户"
}
```

### 2. 用户登录
```bash
POST https://localhost:7000/api/v1/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "123456"
}
```

### 3. 发送消息
```bash
POST https://localhost:7000/api/v1/messages
Authorization: Bearer 你的JWT令牌
Content-Type: application/json

{
  "receiverId": "接收者ID",
  "messageType": "Text",
  "content": "你好，这是一条测试消息！"
}
```

## 🐳 Docker启动（可选）

如果安装了Docker，可以使用以下命令：

```bash
# 启动所有服务（数据库、Redis、API）
docker-compose up -d

# 查看日志
docker-compose logs -f weidin-api

# 停止服务
docker-compose down
```

## 📖 更多信息

- 详细文档：查看 `README.md`
- 数据库配置：查看 `WeiDin.API/DatabaseSetup.md`
- API文档：启动后访问 Swagger UI

## 🆘 获取帮助

如果遇到问题：
1. 查看控制台错误信息
2. 检查日志文件：`WeiDin.API/logs/`
3. 确认所有依赖已正确安装
4. 检查防火墙和网络设置

---

**祝您使用愉快！** 🎉
